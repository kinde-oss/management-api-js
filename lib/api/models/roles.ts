export type roles = {
  /**
   * The role's id.
   */
  id?: string;
  /**
   * The role identifier to use in code.
   */
  key?: string;
  /**
   * The role's name.
   */
  name?: string;
  /**
   * The role's description.
   */
  description?: string;
};
